[package]
name = "lovr-rs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.11", features = ["luajit", "module"] }
libc = "0.2.0"
glam = "0.30.9"

[build-dependencies]
bindgen = "0.72.1"

